# 项目定位

本项目是 WhatsApp AI 销售助手 V1，服务中国跨境电商业务员。

# V1 只做

- WhatsApp Web 侧边栏；
- Desktop / 复制粘贴模式；
- AI 回复；
- 翻译；
- 客户 CRM；
- 产品资料库；
- 报价助手；
- 跟进提醒；
- 首页工作台。

# 明确不做

- 不接入 WhatsApp 官方 API；
- 不自动发送 WhatsApp 消息；
- 不自动群发；
- 不自动加好友；
- 不自动拉群；
- 不绕过 WhatsApp 风控；
- 不做团队/角色/部门权限；
- 不做复杂财务；
- 不做采购预测；
- 不做老板驾驶舱；
- 不做完整订单系统。

# 安全边界

- AI 只生成草稿；
- 业务员必须手动确认后发送；
- 不编造价格、库存、交期、运费、付款状态；
- 涉及价格、库存、交期、运费、付款时必须提醒业务员确认；
- 所有用户数据必须按当前登录用户隔离。

# 开发要求

- 每个功能必须有测试；
- 每个模块必须更新 README 或 Known Issues；
- 不允许范围蔓延；
- 每次任务完成必须输出修改文件、测试结果、已知问题；
- 如发现自动发送或群发逻辑，必须立即移除。
